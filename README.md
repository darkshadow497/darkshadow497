# Multiplayer Ping Pong Web Game

A lightweight browser-based multiplayer ping pong game.

## Run locally

```bash
npm start
```

Open `http://localhost:3000` in two browser tabs/windows to play as left and right paddles.
Additional tabs become spectators.

## Controls

- `W/S`
- `Arrow Up/Arrow Down`
